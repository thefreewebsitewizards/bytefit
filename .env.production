# Production Environment Variables for Vercel Deployment
# These variables will be automatically used when NODE_ENV=production

# Firebase Functions URL (Production)
REACT_APP_FIREBASE_FUNCTIONS_URL=https://us-central1-bytefit-v2.cloudfunctions.net

# API Base URL (same as Firebase Functions URL)
REACT_APP_API_BASE_URL=https://us-central1-bytefit-v2.cloudfunctions.net

# Disable ESLint warnings during build
ESLINT_NO_DEV_ERRORS=true
GENERATE_SOURCEMAP=false

# Note: 
# - Only REACT_APP_ prefixed variables are accessible in React frontend
# - Backend environment variables are configured in Firebase Functions using:
#   firebase functions:config:set or Firebase Secret Manager
# - Stripe keys and other secrets are stored in Firebase Functions config
# - Firebase config (API keys, etc.) are in src/config/firebase.ts